<h1>Items</h1>

<% @items.each do |item| %>
  <%= item.name %><br>
  <%= item.price %><br>
  <%= item.category %><br>
  <%= item.description %><br>
  <%= item.shop_location %><br>
  <%= item.available %>
  <%#= item.photo cl_image_tag %>
  <%#= link_to "Show", #_path %>
<% end %>
